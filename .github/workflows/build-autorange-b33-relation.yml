name: Build AutoRange B3.3 Relation x86

on:
  workflow_dispatch:
  push:
    branches:
      - agent/autorange-b33-relation-20260810
    paths:
      - 'tools/apply_autorange_b33_relation.py'
      - '.github/workflows/build-autorange-b33-relation.yml'

permissions:
  contents: read

jobs:
  build-x86:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Checkout build branch
        uses: actions/checkout@v4

      - name: Install Clang and build tools
        shell: bash
        run: |
          set -euxo pipefail
          sudo apt-get update
          sudo apt-get install -y curl ca-certificates gnupg make unzip git patch file python3
          curl -fsSL https://apt.llvm.org/llvm.sh -o llvm.sh
          chmod +x llvm.sh
          sudo ./llvm.sh 19
          sudo apt-get install -y lld-19
          sudo ln -sf /usr/bin/clang-cl-19 /usr/local/bin/clang-cl
          sudo ln -sf /usr/bin/lld-link-19 /usr/local/bin/lld-link

      - name: Fetch exact UnitXP_SP3 v89 source
        shell: bash
        run: |
          set -euxo pipefail
          mkdir upstream
          git -C upstream init
          git -C upstream remote add origin https://github.com/brues-code/UnitXP_SP3.git
          git -C upstream fetch --depth 1 origin 3370011e396ccdbf4c9921f6cf2b005bed818b24
          git -C upstream checkout --detach FETCH_HEAD
          test "$(git -C upstream rev-parse HEAD)" = "3370011e396ccdbf4c9921f6cf2b005bed818b24"

      - name: Rebuild exact TRUE 8x8 source
        shell: bash
        run: |
          set -euxo pipefail
          find upstream -maxdepth 1 -type f \( -name '*.cpp' -o -name '*.h' -o -name 'Makefile' \) -print0 | xargs -0 sed -i 's/\r$//'
          cat \
            patches/true8x8.patch.gz.b64.part00 \
            patches/true8x8.patch.gz.b64.part01 \
            patches/true8x8.patch.gz.b64.part02.00 \
            patches/true8x8.patch.gz.b64.part02.01.00 \
            patches/true8x8.patch.gz.b64.part02.01.01 \
            patches/true8x8.patch.gz.b64.part02.01.02 \
            patches/true8x8.patch.gz.b64.part02.01.03 \
            patches/true8x8.patch.gz.b64.part02.02 \
            patches/true8x8.patch.gz.b64.part02.03 \
            patches/true8x8.patch.gz.b64.part03 \
            patches/true8x8.patch.gz.b64.part04 \
            patches/true8x8.patch.gz.b64.part05 \
            patches/true8x8.patch.gz.b64.part06 \
            > true8x8.patch.gz.b64
          echo 'a932b4a1b9786c10e23755ee344fb285fd2ceca25333825c6e99cbc105d8c320  true8x8.patch.gz.b64' | sha256sum -c -
          base64 -d true8x8.patch.gz.b64 > true8x8.patch.gz
          echo '19ca05bd7d6943c60b9d4372723c01e77a3ba551afcf300172f914e0c23b844b  true8x8.patch.gz' | sha256sum -c -
          gzip -dc true8x8.patch.gz > true8x8.patch
          echo 'f5d7506b85955661d322b00b6b2aa3bbfae8601cd7c05e672a8338a8ddc875e4  true8x8.patch' | sha256sum -c -
          patch --dry-run -p1 -d upstream < true8x8.patch
          patch --batch --forward -p1 -d upstream < true8x8.patch

      - name: Apply GroundProbe B1 DynamicObject probe
        shell: bash
        run: |
          set -euxo pipefail
          echo '27f87c231bf5c174201c7e26ddc298e827435363e8d5135dece510f40795be7c  patches/groundprobe-b1.patch' | sha256sum -c -
          patch --dry-run -p1 -d upstream < patches/groundprobe-b1.patch
          patch --batch --forward -p1 -d upstream < patches/groundprobe-b1.patch

      - name: Apply stable B2 caster resolver
        shell: bash
        run: |
          set -euxo pipefail
          python3 -m py_compile tools/apply_groundprobe_b2_fixed.py
          python3 tools/apply_groundprobe_b2_fixed.py upstream
          grep -Fq 'GroundProbe.UnitByGuid' upstream/dllmain.cpp

      - name: Apply AutoRange B3 base
        shell: bash
        run: |
          set -euxo pipefail
          python3 -m py_compile tools/apply_autorange_b3.py
          python3 tools/apply_autorange_b3.py upstream
          grep -Fq 'AutoRange.Resolve' upstream/dllmain.cpp

      - name: Apply B3.1 raw client DBC offsets
        shell: bash
        run: |
          set -euxo pipefail
          python3 -m py_compile tools/apply_autorange_b31_fix.py
          python3 tools/apply_autorange_b31_fix.py upstream
          grep -Fq 'Schema=CLIENT1121' upstream/AutoRange.cpp

      - name: Apply B3.2 harmful geometry scoring
        shell: bash
        run: |
          set -euxo pipefail
          python3 -m py_compile tools/apply_autorange_b32_resolver.py
          python3 tools/apply_autorange_b32_resolver.py upstream
          grep -Fq 'effectGeometryScore' upstream/AutoRange.cpp
          grep -Fq 'candidateScore' upstream/AutoRange.cpp
          grep -Fq 'Schema=CLIENT1121-B32' upstream/AutoRange.cpp

      - name: Apply legacy UnitXP speed API
        shell: bash
        run: |
          set -euxo pipefail
          python3 -m py_compile tools/apply_speed_api.py
          python3 tools/apply_speed_api.py upstream
          grep -Fq 'cmd == "speed"' upstream/dllmain.cpp
          grep -Fq 'unit + 0xA2C' upstream/dllmain.cpp
          grep -Fq 'unit + 0xA34' upstream/dllmain.cpp
          grep -Fq 'unit + 0xA3C' upstream/dllmain.cpp

      - name: Apply B3.3 native relation classifier
        shell: bash
        run: |
          set -euxo pipefail
          python3 -m py_compile tools/apply_autorange_b33_relation.py
          python3 tools/apply_autorange_b33_relation.py upstream
          grep -Fq 'AutoRange.RelationByGuid' upstream/dllmain.cpp
          grep -Fq 'vanilla1121_unitReaction(object)' upstream/AutoRange.cpp
          grep -Fq 'vanilla1121_unitCanBeAttacked(object)' upstream/AutoRange.cpp
          grep -Fq 'UNKNOWN_DEFAULT_SHOW' upstream/AutoRange.cpp

      - name: Install xwin
        shell: bash
        run: |
          set -euxo pipefail
          XWIN_VERSION=0.9.0
          curl -L --fail --retry 3 "https://github.com/Jake-Shadle/xwin/releases/download/${XWIN_VERSION}/xwin-${XWIN_VERSION}-x86_64-unknown-linux-musl.tar.gz" -o xwin.tar.gz
          tar -xzf xwin.tar.gz
          sudo install -m 0755 "xwin-${XWIN_VERSION}-x86_64-unknown-linux-musl/xwin" /usr/local/bin/xwin

      - name: Download Microsoft CRT and Windows SDK x86
        shell: bash
        run: |
          set -euxo pipefail
          xwin --accept-license --arch x86 splat --output upstream/xwinSDK

      - name: Download Microsoft D3DX headers
        shell: bash
        run: |
          set -euxo pipefail
          curl -L --fail --retry 3 "https://www.nuget.org/api/v2/package/Microsoft.DXSDK.D3DX/9.29.952.8" -o d3dx.nupkg
          unzip -q d3dx.nupkg -d d3dx_pkg
          mkdir -p upstream/d3d9x_headers
          cp -a d3dx_pkg/build/native/include/. upstream/d3d9x_headers/
          test -f upstream/d3d9x_headers/d3dx9.h

      - name: Build PE32 x86 DLL
        working-directory: upstream
        shell: bash
        run: |
          set -euxo pipefail
          make clean
          make -j2
          test -f UnitXP_SP3.dll
          file UnitXP_SP3.dll | tee DLL-FILE-TYPE.txt
          grep -Eq 'PE32 executable.*Intel 80386|PE32 executable.*80386' DLL-FILE-TYPE.txt
          python3 - <<'PY'
          from pathlib import Path
          data = Path('UnitXP_SP3.dll').read_bytes()
          assert b'speed\x00' in data, 'speed command string missing from DLL'
          assert b'UNKNOWN_DEFAULT_SHOW\x00' in data, 'unknown fallback marker missing from DLL'
          assert b'HOSTILE_SIGNAL\x00' in data, 'hostile relation marker missing from DLL'
          assert b'FRIENDLY_REACTION\x00' in data, 'friendly relation marker missing from DLL'
          print('B3.3 relation logic markers + speed marker present in DLL')
          PY

      - name: Package B3.3 relation test artifact
        shell: bash
        run: |
          set -euxo pipefail
          mkdir -p release
          cp upstream/UnitXP_SP3.dll release/UnitXP_SP3_B33_Relation_Test.dll
          cp upstream/UnitXP_SP3.pdb release/ 2>/dev/null || true
          cp upstream/DLL-FILE-TYPE.txt release/
          sha256sum release/UnitXP_SP3_B33_Relation_Test.dll > release/SHA256.txt
          cat > release/README-B33-Relation-Test.txt <<'TXT'
          UnitXP_SP3 AutoRange B3.3 native relation test

          Baseline chain:
          TRUE 8x8 + GroundProbe B1/B2 + AutoRange B3/B3.1/B3.2 + legacy speed API.
          B3.3 adds only the AutoRange native tri-state relation classifier.

          New API:
          UnitXP("AutoRange.RelationByGuid", guidString)

          Return examples:
          R|HOSTILE|reaction|canAttack|objectType|GUID|HOSTILE_SIGNAL
          R|FRIENDLY|reaction|canAttack|objectType|GUID|FRIENDLY_REACTION
          R|UNKNOWN|reaction|canAttack|objectType|GUID|reason

          Intended AutoRange policy:
          FRIENDLY -> filter completely
          HOSTILE  -> show danger circle
          UNKNOWN  -> show danger circle (fail-safe; never hide on uncertain relation)

          Classification:
          self -> FRIENDLY
          canAttack == 1 -> HOSTILE
          HATED/HOSTILE/UNFRIENDLY reaction -> HOSTILE
          AMIABLE/FRIENDLY/REVERED reaction -> FRIENDLY
          neutral, unreadable, invisible, invalid, contradictory -> UNKNOWN

          The relation API is designed to be called from UNIT_CASTEVENT for the event caster.
          It does not introduce a per-frame 40-unit Lua scan.

          Quick target diagnostic (requires UnitGUID support):
          /run local g=UnitGUID("target"); DEFAULT_CHAT_FRAME:AddMessage(tostring(UnitXP("AutoRange.RelationByGuid",tostring(g))))

          Existing CAT compatibility API remains present:
          UnitXP("speed", "player")

          This artifact is compile/static verified only. In-game behavior still requires real-client testing.
          TXT

      - name: Upload B3.3 relation test artifact
        uses: actions/upload-artifact@v4
        with:
          name: UnitXP-SP3-B33-Relation-x86
          path: release/
          if-no-files-found: error
          retention-days: 14
