name: HFAMapUniversal v1.9.3 CustomSwitchActionRuntimeProbe

on:
  push:
    branches:
      - feature/login-ip-tracer-dylib-v2-hfamap187-v193
    paths:
      - 'hfamap/**'
      - '.github/scripts/hfamap_v189_patch.py'
      - '.github/scripts/hfamap_v192_patch.py'
      - '.github/scripts/hfamap_v193_patch.py'
      - '.github/workflows/theos-hfamap-v193.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install build dependencies
        run: brew install ldid

      - name: Install Theos
        run: |
          git clone --recursive https://github.com/theos/theos.git $HOME/theos
          echo "THEOS=$HOME/theos" >> $GITHUB_ENV

      - name: Apply v1.9.3 CustomSwitchActionRuntimeProbe
        run: |
          python3 .github/scripts/hfamap_v189_patch.py
          python3 .github/scripts/hfamap_v192_patch.py
          python3 .github/scripts/hfamap_v193_patch.py
          git diff --check -- hfamap/src/HFAMapPatchExecutionTrace.m hfamap/src/HFAMapLegacy.m
          git diff -- hfamap/src/HFAMapPatchExecutionTrace.m hfamap/src/HFAMapLegacy.m

      - name: Build and verify HFAMapUniversal v1.9.3
        run: |
          cd hfamap
          make clean
          make FINALPACKAGE=1
          mkdir -p dist
          out="dist/HFAMapUniversal_v1.9.3_CustomSwitchActionRuntimeProbe.dylib"
          cp .theos/obj/HFAMapUniversal.dylib "$out"
          file "$out"
          shasum -a 256 "$out" | tee dist/SHA256_v1.9.3.txt
          strings "$out" | grep -q "HFALearn v1.9.3 CustomSwitchActionRuntimeProbe"
          strings "$out" | grep -q "HFALearn UI v1.9.3 CustomSwitchActionRuntimeProbe"
          strings "$out" | grep -q "\[CUSTOM-ACTION-HOOK\]"
          strings "$out" | grep -q "\[CUSTOM-ACTION-RUNTIME\]"
          strings "$out" | grep -q "\[CUSTOM-ACTION-FLOW\]"
          strings "$out" | grep -q "\[CUSTOM-PATH-BEGIN\]"
          strings "$out" | grep -q "\[CUSTOM-PATH-CALL\]"
          strings "$out" | grep -q "\[CUSTOM-PATH-INDIRECT\]"
          strings "$out" | grep -q "\[CUSTOM-STATE\]"
          strings "$out" | grep -q "\[FULL-MAPPING\]"
          strings "$out" | grep -q "\[PACKAGE-EXPORT\]"
          strings "$out" | grep -q "HFAMap_Mapping.log"
          ls -lh "$out"

      - name: Publish raw dylib
        run: |
          git checkout -- hfamap/src/HFAMapPatchExecutionTrace.m hfamap/src/HFAMapLegacy.m
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f hfamap/dist/HFAMapUniversal_v1.9.3_CustomSwitchActionRuntimeProbe.dylib hfamap/dist/SHA256_v1.9.3.txt
          git commit -m "Publish HFAMapUniversal v1.9.3 dylib [skip ci]" || exit 0
          git push origin HEAD:feature/login-ip-tracer-dylib-v2-hfamap187-v193

      - name: Upload build output
        uses: actions/upload-artifact@v4
        with:
          name: HFAMapUniversal-v1.9.3-CustomSwitchActionRuntimeProbe
          path: |
            hfamap/dist/HFAMapUniversal_v1.9.3_CustomSwitchActionRuntimeProbe.dylib
            hfamap/dist/SHA256_v1.9.3.txt
          if-no-files-found: error
