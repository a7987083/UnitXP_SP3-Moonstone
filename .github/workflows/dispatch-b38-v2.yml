name: Dispatch corrected B3.8 build

on:
  push:
    branches:
      - agent/autorange-b38-cleancore-20260810
    paths:
      - '.github/workflows/dispatch-b38-v2.yml'

permissions:
  actions: write
  contents: read

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch B3.8 v2 workflow at latest branch
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run build-autorange-b38-cleancore-v2.yml --ref agent/autorange-b38-cleancore-20260810
