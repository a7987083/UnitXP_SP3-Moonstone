--- a/MoonMarker/MoonMarker.lua
+++ b/MoonMarker/MoonMarker.lua
@@ -187,6 +187,10 @@
     if event == "CHAT_MSG_ADDON" then
         local prefix, message, channel, sender = arg1, arg2, arg3, arg4
         if prefix ~= PREFIX or not HasDLL() then return end
+        local playerName = UnitName("player")
+        if sender and playerName and string.lower(sender) == string.lower(playerName) then
+            return
+        end
         if message == "CLEAR" then
             pcall(UnitXP, "MoonMarker.Clear")
             return
