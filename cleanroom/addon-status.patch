diff -ruN addon_base/MoonMarker/MoonMarker.lua addon_new/MoonMarker/MoonMarker.lua
--- addon_base/MoonMarker/MoonMarker.lua	2026-08-01 17:38:17.965552821 +0000
+++ addon_new/MoonMarker/MoonMarker.lua	2026-08-01 18:33:02.897671606 +0000
@@ -89,13 +89,14 @@
         Print("未检测到新版 UnitXP_SP3.dll")
         return
     end
-    local ok, count, draws, projected, texture = pcall(UnitXP, "MoonMarker.Status")
+    local ok, count, draws, projected, texture, hook = pcall(UnitXP, "MoonMarker.Status")
     if not ok then
         Print("状态查询失败：" .. tostring(count))
         return
     end
-    Print(string.format("记录=%s，渲染帧=%s，当前投影=%s，纹理=%s",
-        tostring(count), tostring(draws), tostring(projected), texture and "已加载" or "未加载"))
+    Print(string.format("记录=%s，Present钩子=%s，渲染帧=%s，当前投影=%s，纹理=%s",
+        tostring(count), hook and "已安装" or "未安装", tostring(draws), tostring(projected),
+        texture and "已加载" or "未加载"))
 end
 
 local frame = CreateFrame("Frame", "MoonMarkerFrame", UIParent)
