--- a/sceneBegin_sceneEnd.cpp	2026-08-01 18:22:26.549648595 +0000
+++ b/sceneBegin_sceneEnd.cpp	2026-08-01 18:22:27.121648616 +0000
@@ -493,6 +493,17 @@
         return;
     }
 
+    // The working marker DLL updates/draws its orthographic overlay at the very
+    // beginning of sceneEnd. It must not depend on gxDevice_scenePresent because
+    // some 1.12 clients never toggle scene_isPresenting for this hook path.
+    // Reuse the last valid D3DX sprite/device as soon as they are available.
+    if (scene_lastDXdevice != NULL
+        && scene_sprite != NULL
+        && scene_spriteOnLost == false
+        && scene_needRebuildSprite == false) {
+        moonMarker::render(scene_sprite, scene_lastDXdevice);
+    }
+
     if (scene_isPresenting == false) {
         p_original_sceneEnd(CGxDevice);
         return;
@@ -528,11 +539,6 @@
             return;
         }
 
-        // MoonMarker is intentionally rendered before all font checks. The marker
-        // system requires only the valid D3DX sprite/device and remains available
-        // when floating combat text fonts are disabled or still reloading.
-        moonMarker::render(scene_sprite, scene_lastDXdevice);
-
         if (scene_fallback == NULL || scene_fallbackBIG == NULL || scene_fallbackSmall == NULL || scene_fallbackHUGE == NULL
             || scene_selected == NULL || scene_selectedBIG == NULL || scene_selectedSmall == NULL || scene_selectedHUGE == NULL) {
             scene_needReloadFont = true;
