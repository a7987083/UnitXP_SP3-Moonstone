--- nativeM2Test.h.orig
+++ nativeM2Test.h
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <cstdint>
 #include <string>
 
@@ -87,6 +88,18 @@
 void clearAdvancedPreview();
 AdvancedPreviewStatus advancedPreviewStatus();
 
+// Creates/replaces one persistent advanced top-model slot for a beam color.
+// These slots are separate from the editor preview and can coexist for all 8 colors.
+bool createAdvancedTopModel(const std::string& colorName,
+                            const std::string& modelPath,
+                            const C3Vector& position,
+                            float scale,
+                            float yawDegrees,
+                            std::string& normalizedPath);
+bool clearAdvancedTopModel(const std::string& colorName);
+void clearAdvancedTopModels();
+std::size_t advancedTopModelCount();
+
 // Removes one color slot without affecting the other seven.
 bool clearColor(const std::string& colorName);
 
